function setup() { 
  createCanvas(400, 400);
} 

function draw() { 
  background(255);
	strokeWeight(1);
	stroke(0);
	
	var x = 0;

	
while (x <=200) {
	line(x, 200 + x, 400 - x, 200 + x);
	x = x + 5;
	line(x, 200 - x, 400 - x, 200 - x);
	x = x + 5;
}
}
