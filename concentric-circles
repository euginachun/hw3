function setup() { 
  createCanvas(400, 400);
} 

function draw() { 
  background(255);
	strokeWeight(1);
	stroke(0);
	
	var x = 0;

	
while (x < width) {
	noFill();
	ellipse(200, 200, x, x);
	x = x + 10;
}
}
